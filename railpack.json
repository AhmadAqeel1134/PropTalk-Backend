{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.11.9"
  },
  "deploy": {
    "startCommand": "sh -c \"alembic upgrade head && exec python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}\""
  }
}
